#include "header.h"

// QUICK NOTE:
// Proof-of-Concept implemented in ~10 minutes. Code is intentionally minimal and not production-ready.
// TODO: improve error handling, use RAII for handles, sanitize input, remove exits from library code,
//       avoid downloading critical auth data from raw URLs, add logging and tests.

int main() {

	dologin();

	system("pause");
	

}
